@{
    ViewData["Title"] = "Página inicial";
}
@model UsuarioModel
<div class="text-center">
    <h1 class="display-4">Deseja realmente deletar o usuário (@Model.Nome)?</h1>
    <a role="button" class="btn btn-primary" asp-controller="Usuario" asp-route-id="@Model.Id" asp-action="ApagarMoto">Excluir</a>
    <a role="button" class="btn btn-danger" asp-controller="Usuario" asp-action="Index">Voltar</a>
</div>